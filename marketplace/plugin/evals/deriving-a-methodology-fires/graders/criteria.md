---
type: llm
---

PASS if the answer does all four:

1. Names a method whose output is a how, before offering any how itself: one of Native's own (the Lab card, the eight-step work-system procedure, the bootstrap's place step, the decision procedure, the inner engine, process discovery) or a named general method for the tier.
2. States a budget for the climb as numbers: steps, time or tokens.
3. Says how the produced how gets written back as a workflow type, and where, so the next instance does not climb again.
4. Says what the produced how must contain to count: named steps, each with a check that could fail.

FAIL if the answer jumps to a design (a schema, a state machine, a contract) with no method named first, or names no budget, or treats writing the result back as optional.
