---
type: tool_used
tool: Skill
input_match: '"skill"\s*:\s*"(?:[\w-]+:)?native-method"'
---

The method must fire on "how would we know" without being named. native-coach firing
instead also routes to the method; score that as a description overlap, not a pass.
