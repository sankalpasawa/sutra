#!/bin/sh
# RED FIXTURE MEMBER - intentionally shipped WITHOUT the exec bit (0644).
# This file exists to be rejected. validate-hook-paths.sh --fixture and
# runtime/selftest.sh must both report it. Never chmod +x this file.
exit 0
