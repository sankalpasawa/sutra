# .js files committed alongside .ts so plugin install path works without npm install.
# `tsc -p tsconfig.build.json` regenerates them; release prerelease guard asserts parity.
dist/
node_modules/
*.tsbuildinfo
