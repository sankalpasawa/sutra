{
  "name": "core",
  "version": "2.39.7",
  "description": "v2.39.7 (2026-06-11) — H-Sutra Stop hook no longer false-fires on turns that invoke a Skill: is_human_user() now filters isMeta user rows (skill invocations + stop-hook feedback are role:user/isMeta=true, not human turns), so the header check targets the real first response. Operating system for building with AI. See CHANGELOG.md.",
  "author": {
    "name": "Sutra",
    "url": "https://sutra.os"
  },
  "homepage": "https://sutra.os",
  "license": "MIT",
  "keywords": [
    "governance",
    "input-routing",
    "depth-estimation",
    "readability",
    "workflow",
    "opinionated-os",
    "session-discipline"
  ],
  "userConfig": {
    "profile": {
      "type": "string",
      "title": "Enforcement profile",
      "description": "individual = warn-only, telemetry off. project = warn-only, telemetry on (default). company = HARD block on missing depth markers, telemetry on.",
      "sensitive": false
    }
  }
}
