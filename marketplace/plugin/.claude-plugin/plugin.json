{
  "name": "core",
  "version": "2.39.6",
  "description": "v2.39.6 (2026-05-31) — prompt-capture hook promoted to plugin L0 (fleet-wide): every UserPromptSubmit appends a lossless JSONL row to the project's `holding/state/prompts/<YYYY-MM>.jsonl` so the prompt trail survives the session transcript. Non-blocking; kill via PROMPT_CAPTURE_DISABLED=1. Operating system for building with AI. See CHANGELOG.md.",
  "author": {
    "name": "Sutra",
    "url": "https://sutra.os"
  },
  "homepage": "https://sutra.os",
  "license": "MIT",
  "keywords": [
    "governance",
    "input-routing",
    "depth-estimation",
    "readability",
    "workflow",
    "opinionated-os",
    "session-discipline"
  ],
  "userConfig": {
    "profile": {
      "type": "string",
      "title": "Enforcement profile",
      "description": "individual = warn-only, telemetry off. project = warn-only, telemetry on (default). company = HARD block on missing depth markers, telemetry on.",
      "sensitive": false
    }
  }
}
