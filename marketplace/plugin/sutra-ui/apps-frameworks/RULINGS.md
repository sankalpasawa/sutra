# Apps frameworks kit -- rulings

Decisions the kit is built on. Each one is a ruling, not a preference: a later change needs a new ruling here and a note in the design doc.

| Field | Value |
|---|---|
| **status** | ACTIVE -- v1 (design `holding/departments/experience/desktop-app/2026-09-11-apps-frameworks-design.md`, direction D75) |
| **updated** | 2026-09-12 |
| Source of ids | `angles/*.json` (the only authored source); `*.rules.md` and `screens.json` are generated by `build_kit.py` |

## The record stays local and is reconstructed later

`APP.md` is never added to the export whitelist (`module.json`, `index.html`, `assets/**`). The stamp is mirrored into `module.json.frameworkKit`, where unknown fields survive. Reconstructing an `APP.md` skeleton on import lands with the Publish program; until then an imported app carries `not recorded` cells that C1 reports as WARN, never FAIL.

## The server owns the stamp

`frameworkKit` (kit, version, digest, created_at, kind, migrated) is written by `create_module` at create, once, into `APP.md` line 1 and `module.json`. Neither the chat nor `check.py` writes it. An accepted migration rewrites `version`, `digest` and `migrated` in both copies and nothing else: no app `version` bump, no `updated_ms`, no `app.edited`.

## The server materializes the folder first

"+ New app" picks the kind in the panel and calls `create_module` with kind and department. The server writes the kind's starter files, `APP.md` with the stamp, then `module.json` last. The chat opens inside the folder and fills answers, the surface, `name` and `tagline` only. The seed carries no manifest fields, no ref, no schema, no status.

## The waiver is the only override

`check.py --waive <id> --why "<reason>"` records a waiver for one must-fix check in the `## Checks` block and shows it in the header chip. There is no blanket bypass and no environment flag. `mark_ready` treats a waived check as passing.

## A link is created ready

A link's whole surface is one screen id the server validates at create, so a draft state protects nothing. PR-6 (stay draft until the success line is true) applies to page and chat only.

## v1 is the basic cut

v1 asks the six required questions per kind plus one prefilled confirm message. Eighteen checks block "done" (`build_kit.V1_MUST_FIX`); every other check ships as `suggest` and may be promoted from field data. The designed `level` stays in the JSON as the target for later promotion.

## Nothing founder-only reaches builders

No block names, decision ids, ADR numbers, routing-pin text or `dref-` in any seed, builder-facing kit file, `APP.md` or app surface. C4 is the single home of that rule and its scanner runs over the shipped builder-facing files in the tests. Design question ids are `DS1..DS6` (not `D1..D6`) so a record never trips the scanner's decision-id pattern.

---
provenance: written 2026-09-12 by Claude (session c0a2923c, atom a-c0a2923c-23) from design v1 §Open questions and §v1 scope; program `holding/plans/apps-frameworks/PROGRAM.md` step 2.
