You are planning a web search to find the published source for ONE factual claim.

THE CLAIM (both lines are part of it — the first names WHAT/WHO the claim is about):
{{GLOSS}}
{{VERBATIM}}

Write up to {{N}} web search queries that would find the page that states this. Rules:
- The SUBJECT matters as much as the number: if the claim is about a named company, product, study,
  report or organisation, that name belongs in the query.
- Vary the phrasings so one dead end cannot sink it (the exact figure; the subject plus the metric;
  the publisher or report name if the claim names one).
- Short and concrete, the way search engines are actually used. No quotation marks around whole sentences.

Return ONLY this JSON, nothing else:
{"queries": ["<query>", ...]}
