The step before you rewrote this whole article to be readable. It did most of the job and left a
few paragraphs too long. You are fixing only those, and nothing else.

A paragraph runs up to 4 sentences. Anything longer is a wall a reader skims past, and the ones
below are over.

────────────────────────────────────────────────────────────────────────
YOUR TWO JOBS, IN THIS ORDER

1. SHORTEN IT. This is what you try first, every time. A paragraph that runs to six sentences is
   usually four sentences of argument and two of scaffolding: a line that restates the one above,
   a line that sets up a fact which explains itself, a line that winds down after the point has
   landed. Cut those and the paragraph is the right length with nothing lost.

2. SPLIT IT — only when shortening genuinely cannot get there. A paragraph that holds two separate
   ideas should be two paragraphs. Then:
     · ONE split. A paragraph becomes two. Never three.
     · Only if the section has room. Each paragraph below tells you how many paragraphs its
       section currently has. A SECTION MAY NOT EXCEED 4 PARAGRAPHS. If its section is already at
       4, you have no room — shorten instead, however hard.

Do not add a sub-heading. Do not move the paragraph to another section. Do not touch a single word
outside the paragraphs listed.

────────────────────────────────────────────────────────────────────────
WHAT MUST SURVIVE, IN EVERY PARAGRAPH YOU HAND BACK

· THE SOURCE TAGS — [c412], [c88, c91]. Every tag that is in the paragraph now is in the paragraph
  you return, still attached to the claim it proves. Never invent one. Never move one onto a
  different fact. If you delete a sentence, its tag goes with it — that is the only way a tag leaves.

· THE FIGURES. Every number stays, and you may not introduce one that is not already there. You
  cannot look anything up. If you delete a sentence, its figure goes with it.

· THE MARKDOWN. A link stays a link, address unchanged. A table stays a table. A list stays a list.

· THE MEANING. The paragraph you return says what the paragraph you were given said, in fewer words.
  It is not a new argument.

────────────────────────────────────────────────────────────────────────
IT HAS TO DROP STRAIGHT BACK IN

Your replacement is pasted into the article exactly where the old paragraph sat, by a script, with
nothing else changed. So it has to read correctly against the paragraph above it and the paragraph
below it — both of which you can see in the full article. Do not repeat what the paragraph above
already said. Do not use "as mentioned earlier" or any other bridge; write it so it simply fits.

Where you split into two, hand back both paragraphs in one string, separated by a blank line.

────────────────────────────────────────────────────────────────────────
THE PARAGRAPHS TO FIX

{{TARGETS}}

────────────────────────────────────────────────────────────────────────
THE WHOLE ARTICLE, so your fixes read correctly in place. The paragraphs you are fixing are marked
with their id. Everything else here is context — you may not change it.

{{ARTICLE}}

────────────────────────────────────────────────────────────────────────
════════════════════════════════════════════════════════════════════════
THE USER'S STANDING RULES. They were set by the person publishing this and they win over any
rule above that they contradict. "(none)" means there are none.
{{MEMORY}}

Return ONLY the replacements, keyed by id. Nothing else, and no paragraph that was not listed.

{"fixes": [{"id": "<the id exactly as given>",
            "prose": "<the replacement paragraph — or two, separated by a blank line>",
            "how": "<one short line: shortened, or split and why shortening could not get there>"}]}
