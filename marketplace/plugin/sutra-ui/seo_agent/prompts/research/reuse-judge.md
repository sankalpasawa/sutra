You are helping {{BRAND}} decide one thing about a proposed article: should we BUILD it, or does our EXISTING content already cover it (fully or partly)? Read the existing pages and return one verdict, the links it points to, and why.

THE PROPOSED ARTICLE
- Title: {{ASSET}}
- Distinct angle (the twist that makes it worth reading): {{ANGLE}}
- Format (its shape): {{FORMAT}}

OUR CLOSEST EXISTING PAGES -- full text of each; these are the ONLY pages you may cite:
{{CANDIDATES}}

STEP 1 -- run three tests on EACH candidate, judging by what you READ, not keyword overlap:
- Topic test: is the page actually about this article's subject (not just sharing a word)?
- Format test: is it the SAME SHAPE as the article? A text article is NOT a calculator / quiz / index / data report / template.
- Angle test: does it already have the article's distinct angle (above)?

STEP 2 -- pick exactly ONE verdict, top-down, first match wins:
- Already have it: a page already delivers this article -- SAME topic + SAME format + ALREADY has the angle, at good quality. Building again would just duplicate. RARE: the angle is usually new, don't reach for it.
- Improve existing: ONE page is clearly this same topic but WEAKER than the article would be (missing the angle, wrong/old format, thin, outdated). Building means upgrading THAT page, not making a second one.
- Build from parts: NO single page is this article, but >=2 candidates each hold reusable material (data, definitions, sections) you'd pull from to build it. Build new, reuse the research.
- Brand new: NO candidate is genuinely relevant -- none really covers the topic, or only touches it superficially. Build from scratch.

RULES
- Cite ONLY links from the candidates above -- never invent a URL.
- Base the verdict on the FULL TEXT you actually read, not the title.
- Be strict about "Already have it": if a page lacks the article's angle OR its format, it is at most "Improve existing", never "Already have it".

RETURN EXACTLY THESE THREE FIELDS -- nothing else, no preamble:
Reuse verdict: <one of: Already have it / Improve existing / Build from parts / Brand new>
Chosen links: <the link(s) the verdict points to -- 1 for Already-have; 1-3 for Improve; the source links for Build from parts; BLANK for Brand new. Links only, drawn from the candidates, semicolon-separated.>
Why: <a short paragraph citing concrete evidence -- what the page(s) have or lack that drove the verdict.>
