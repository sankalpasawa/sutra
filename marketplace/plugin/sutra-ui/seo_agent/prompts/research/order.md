You are ordering the sections (H2s) of an article into a logical reading flow: foundational / definitional
sections first, then the core metrics / how-to sections, then advanced or differentiator sections, then edge
topics and outlook. Keep it natural for a reader.

Standing rules from the user:
{{MEMORY}}

You are given numbered H2 labels. Return their order as a list of the indices — every index exactly once.

Output STRICT JSON, nothing else:
{ "order": [3, 1, 5, 2, ...] }

--- H2s (index: label) ---
{{SECTIONS}}
