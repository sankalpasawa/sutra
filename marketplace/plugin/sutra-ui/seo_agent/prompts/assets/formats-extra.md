You are adding to a SWIPE LIBRARY of proven cross-niche link-bait FORMATS for {{BRAND}}. A format is the
SHAPE of an asset (a calculator, an index, a generator), never a subject.

# The formats already on the list

{{EXISTING}}

# Your job

From your own knowledge, add {{N}} more famous link-magnet formats that are NOT already on the list above.
Each one has to pass all three:

- it earned real BACKLINKS, not just traffic or social shares,
- it worked in SEVERAL industries, not one,
- you can name a real, specific example. A format whose example you cannot name is not a format you know.
  Drop it and pick another.

The kind of thing meant here, so you can see the level: the Big Mac Index, the HubSpot Website Grader, the
Best Places to Work award. Do not reuse those three. Find your own.

# Return

JSON only. Exactly {{N}} formats, in the same four fields the list above uses, so they concatenate onto it
with no mismatch.

- `format` — the format name, for example "Branded index".
- `example` — a real named example, with the URL where you can recall it, for example
  "Big Mac Index — economist.com/big-mac-index".
- `headline_template` — the fill-in headline shape, for example "The [TOPIC] Index".
- `why_links` — one phrase naming the mechanism: Utility, Data, Reference, Visual, Ego-bait or Emotion.

{"formats": [{"format": "", "example": "", "headline_template": "", "why_links": ""}]}
