# Prices and hidden facts

> Some of what a company sells is drawn on the page by JavaScript. A crawler never sees it, so it
> is missing from BOTH the site catalogue and the raw HTML — only a person with a browser can read
> it. Testlify's whole pricing table is one of these: verified 2026-07-20 against
> testlify.com/pricing/, every price absent from both sources.
>
> Type those facts here. They are HUMAN-VERIFIED and AUTHORITATIVE. The product-facts builder
> carries them through word for word, and where a crawled page disagrees with this file, this file
> wins — stale translated pages and competitor-comparison pages often carry last year's prices.
>
> Saving this file rebuilds `features.md` on its own, from the pages already read. Nothing is
> crawled again, and nothing else in the pack changes.

## Pricing and plans

*(Nothing typed here yet. Replace this line with your plans, what each one costs, what is included,
and how long the trial runs. Plain sentences are fine — write it the way you would say it.)*

## Anything else a crawler cannot see

*(Optional. Add-ons, limits, discounts, the money-back guarantee, the fair-usage policy — whatever
your site renders with JavaScript.)*
