Write the complete `brand-voice.md` for {{BRAND}} ({{NICHE}}) by filling the SCHEMA below from the
EVIDENCE ROWS below. This is PURE ASSEMBLY: every line must trace to an evidence row (a trait observed,
or a verbatim excerpt) or to a stated brand fact. Nothing is invented.

The section -> evidence mapping. This table is lifted VERBATIM from the recipe — follow its
"How to write it" column to the letter (counts, sub-fields, "multi-paragraph", "short label" — every word
of it is a requirement, not a suggestion):

{{MAPPING}}

Naming rule (earned by A/B against the hand-built document): pillar names and the tone label must sit in
THIS company's domain vocabulary and, where the site itself names an enemy (e.g. resumes, gut feel), the
name should carry that fight. Generic role-words ("Teacher", "Guide", "Partner" without the domain) are
weaker than domain-anchored names. Name the pillar LAST, after clustering (substance before label).

Quality standards (earned by an independent A/B review, 2026-07-19):
- "Avoid" lines must be TESTABLE checks a reviewer can run, never sentiments.
- Fairness language stays legally careful: "reduce bias", "a level playing field" — NEVER "bias-free",
  "eliminates/removes bias", or any absolute claim about the product.
- Prefer OBSERVED site copy (quoted, page named) over invented illustrations, everywhere.
- Any numeric style rule you cannot ground in the evidence gets a `> ⚑ HUMAN DECISION:` flag.
- Voice Examples ✅: give TWO excerpts when the evidence supports it — one product/pricing register and
  one editorial/blog register showing hook construction — each verbatim with its page named.

Hard rules:
- No `[BRACKET]` placeholder may survive. Every header in the schema appears, filled.
- Use {{BRAND}}'s REAL numbers, product names and CTA text from the evidence — no vague filler.
- Unevidenced -> `> ⚑ HUMAN DECISION: <what is missing>` — never fabricate.
{{REDO_NOTES}}

Return ONLY the finished markdown document. No preamble, no commentary, no code fences around the whole doc.

THE SCHEMA TO FILL:
{{SCHEMA}}

BRAND FACTS (from the company record):
{{BRAND}} — {{ONELINER}}

THE EVIDENCE ROWS (one per page read):
{{EVIDENCE}}
