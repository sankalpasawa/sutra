Write the complete `features.md` for {{BRAND}} ({{NICHE}}) by filling the SCHEMA below from the FACTS
POOL below. This is a FACTS document — its whole value is accuracy. Every fact comes from the pool;
nothing is invented; an unevidenced section gets `> ⚑ HUMAN DECISION: <what's missing>`.

The consolidation method (lifted verbatim from the recipe — follow it):
{{METHOD}}

The section mapping (lifted verbatim — follow the middle column across ALL page blocks):
{{MAPPING}}

Also:
- Pitch lines (Conversion Angles, trial messages) are worded ON-VOICE per the brand-voice excerpt below.
- Fairness language stays legally careful ("reduce bias", never "bias-free"/"eliminates bias").
- No [BRACKET] placeholder survives; every schema section appears.
{{REDO_NOTES}}
Return ONLY the finished markdown document. No preamble, no code fences around the whole doc.

HUMAN-VERIFIED SEED (AUTHORITATIVE — carry these facts through verbatim; where the crawled pool
disagrees with the seed, the SEED WINS and the crawled variant is not mentioned as a conflict.
Stale translated pages and competitor-comparison pages often carry old prices — the seed is current):
{{SEED}}

THE SCHEMA:
{{SCHEMA}}

BRAND VOICE (for pitch wording only):
{{VOICE}}

THE FACTS POOL (one block per page):
{{FACTS}}
