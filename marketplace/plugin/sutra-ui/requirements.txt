fastapi==0.128.8
uvicorn==0.39.0
websockets==15.0.1
# The SEO Writer agent (seo_agent/). Pure-Python wheels only, so the cross-arch
# DMG bundle can install them with --only-binary.
httpx==0.28.1
beautifulsoup4==4.15.0
# SUPPORTED PYTHON: 3.11-3.12, for every build. The floor is cryptography (below: its only wheel
# is cp311-abi3); the ceiling is numpy 2.0.2 (no cp313 wheel). The release DMG bundles 3.12
# (electron/bundle-runtime.sh) and install.sh/run.sh refuse anything outside the range. The stock
# macOS python3 is 3.9 and CANNOT install these pins -- trafilatura 2.2.0 alone needs 3.10.
#
# The page index (seo_agent/tools/_index.py). numpy 2.0.2 ships cp311 and cp312 wheels for both
# macOS arches (arm64 11_0, x86_64 10_9), checked 2026-09-03 with pip download --only-binary=:all:
# --platform. Moving past 3.12 means moving past this pin first.
numpy==2.0.2
# The article extractor, rung 2b of the catalogue's ladder. On the original workflow's own
# catalogue it won 300 of the 334 pages the DOM rung alone did not, so it is nearly the whole of
# the measured extraction gap. Pure-python wheel; its dependencies ship universal2 wheels, so the
# cross-arch bundle installs it with --only-binary. Optional at runtime: extract.py falls back to
# the ladder it had if the import fails.
trafilatura==2.2.0
jsonschema>=4.17  # Apps manifest validation on import (APPS-THREATS.md X-2); explicit checks still run without it
# Apps registry signatures (ADR-041, Publish program P2): ed25519 sign + verify in modules_sign.py. Not a pure-Python
# wheel, but 46.0.0 ships one cp311-abi3 macosx_10_9_universal2 wheel and its cffi pulls per-arch cp312 wheels, so the
# cross-arch --only-binary install resolves for both arches (checked 2026-09-12 with pip download --platform, both).
cryptography==46.0.0
