---
name: dayflow
description: "CEO of DayFlow — Product company, builds the iOS app"
---

# STOP — Wrong Directory

DayFlow sessions **must** run from the `dayflow/` submodule directory.

Tell the user:

```
To start a DayFlow session:
  1. Open a new terminal
  2. cd dayflow/
  3. Start Claude Code there

You're currently in asawa-holding/ (CEO of Asawa workspace).
To work on DayFlow from here with full access, just say so.
```

Do NOT set an active-role file.

## If the user insists

They're CEO of Asawa — full access. Load DayFlow files directly.
