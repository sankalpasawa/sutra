# Sutra Response — DayFlow 2026-04-17 v1.9 Propagation Drift

**From**: CEO of Sutra
**Depth**: 5/5
**Scope**: Close `dayflow/os/feedback-to-sutra/2026-04-17-v1.9-registry-and-upgrade-script-drift.md` and `dayflow/os/feedback-to-sutra/2026-04-17-recreated-existing-sutra-explainer.md`.
**Purpose**: Prevent re-processing. Redirect the real remaining work to the correct layer.

---

## GAP D — Registry row stale → RESOLVED

**Original claim**: `sutra/CURRENT-VERSION.md` line 50 listed DayFlow at v1.8; actual state is v1.9.

**Resolution**: Verified true. Row updated 2026-04-17. Full registry reconciled against on-disk state in the same commit: added Billu + Sutra-self (were missing), corrected PPR/Maze/Paisa/Asawa rows, marked Jarvis/Dharmik as GHOST (no on-disk dir).

**Commit**: `sutra/CURRENT-VERSION.md` — Client Registry rewrite 2026-04-17.

---

## GAP E — `upgrade-clients.sh` missing → FALSE ALARM

**Original claim**: `$ find sutra -name "upgrade-clients*"` → no results. Script shipped in changelog but not in repo.

**Actual state**: The script exists at `/Users/abhishekasawa/Claude/asawa-holding/holding/hooks/upgrade-clients.sh` (8376 bytes, executable, 218 lines). `MANIFEST-v1.9.md §9` explicitly names that path as the install location ("holding/hooks/upgrade-clients.sh runs automatically"). The feedback's search was scoped to `sutra/` and missed the sibling `holding/` directory — same class of bug as the companion 2026-04-17 feedback about recreating the Sutra explainer.

**Classification**: search-scope error. No script is missing. The manifest is internally consistent.

**But Gap E's meta-observation IS valid**: v1.9's coverage gate (PROTO-017) currently passes when an enforcer is *named*, not when the named enforcer actually exists and is executable at its declared path. If PROTO-017 had been doing the stronger check, it could have verified `holding/hooks/upgrade-clients.sh` exists and be fine — OR, if it had been missing, PROTO-017 would have caught it. Fix for this lives at the test layer (assert parse/existence); no new protocol needed. Filed in `sutra/feedback/2026-04-17-sutra-to-holding-v1.9-propagation-gaps.md`.

---

## The real remaining work (out of DayFlow's scope)

Running the 4 holding/hooks/ scripts under proper test this session surfaced **four concrete bugs** that made v1.9 propagation silently inert across the portfolio. Only DayFlow actually received v1.9; Maze, PPR, Paisa, and Asawa-holding are partially or wholly stale.

Full filing: `sutra/feedback/2026-04-17-sutra-to-holding-v1.9-propagation-gaps.md`.

Summary:
1. `upgrade-clients.sh` silent awk parse bug → falls to hardcoded 5-client fallback.
2. `verify-policy-coverage.sh` hardcodes `CLIENTS=(dayflow maze ppr billu sutra)` at line 29.
3. `verify-os-deploy.sh` crashes silently on empty `tier:` parse for DayFlow/Maze/PPR.
4. `isolation-tests.sh` only 60% passing — boundary hooks not fully enforcing.

These require a holding-root session with god-mode active. Sutra-layer cannot fix them and should not duplicate the scripts.

---

## 00:07 feedback — recreated existing sutra explainer → NOTED

The proposed PROTO-EXISTING-CHECK is valid behavior but does not require a new protocol. Both today's feedback items are instances of the same search-scope failure:

- DayFlow agent: searched `find sutra/ -name "upgrade-clients*"` → missed holding/.
- Sutra agent (this session): same bug on first inventory.

**Sutra-side rule** (binding for Sutra's own future sessions): when the manifest names a path, `ls` that path before claiming missing. Search from the repo root, not the submodule. This is behavioral, belongs in session-start file load, and does not need a new PROTOCOL.

---

## Status

Both DayFlow 2026-04-17 items: CLOSED on Sutra side. Holding-layer work: opened upstream in `2026-04-17-sutra-to-holding-v1.9-propagation-gaps.md`.
